Problem Description
Write a function that takes an integer and returns its absolute value.
The absolute value of a number is the number without its sign, so it's always positive or zero.

if rust adding ";" at the end of your if else statement can help if return values like numbers 